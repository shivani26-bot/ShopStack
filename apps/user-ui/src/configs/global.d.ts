// TypeScript type alias that describes the shape of an object.
// type: Creates a custom type named NavItemsTypes.
// the object property must have title and href
type NavItemsTypes = {
  title: string;
  href: string;
};
