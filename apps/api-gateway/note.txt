PayloadTooLargeError: request entity too large
...
expected: 961842,  // ~940 KB
limit: 102400,     // 100 KB
type: 'entity.too.large'

You’re sending a request body of ~940 KB.

The default JSON/body parser limit in Express is 100 KB (102400 bytes).

Since your payload is bigger than that, Express throws PayloadTooLargeError

app.use(express.json({ limit: "10mb" }));
app.use(express.urlencoded({ limit: "10mb", extended: true }));
 add this in auth product service main.ts